function numberToString(num) {
    return num = String(num);
}

function numberToString(num) {
    return '' + num;
}

function numberToString(num) {
    return `${num}`;
}

function numberToString(num) {
    return num.toString();
}